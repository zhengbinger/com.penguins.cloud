spring:
  application:
    name: cloud-penguins-gateway
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_HOST:lcoalhost:8848}
        namespace: penguins
      config:
        server-addr: ${NACOS_HOST:lcoalhost:8848}
        namespace: penguins
#    gateway:
#      routes:
#        - id: baidu
#          uri: https://www.baidu.com
#          predicates:
#            -Path=/baidu


management:
  endpoints:
    web:
      exposure:
        include: '*'

