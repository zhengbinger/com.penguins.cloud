spring:
  application:
    name: penguins-files-provider
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_HOST:localhost:8848}
        namespace: penguins
        group: penguins
      config:
        server-addr: ${NACOS_HOST:localhost:8848}
        group: penguins
        file-extension: yml
        namespace: penguins

#security:
#  oauth2:
#    resource:
#      user-info-uri: http://localhost:11011/current/get
#      id: penguins-files-provider

penguins:
  oss:
    aliyun:
      access-key-id: 1
      access-secret: 1












