spring:
  application:
    name: cloud-penguins-files
  cloud:
    nacos:
      discovery:
        server-addr: { NACOS_HOST: localhost:8848 }
        namespace: penguins
      config:
        server-addr: { NACOS_HOST: localhost:8848 }
        namespace: penguins